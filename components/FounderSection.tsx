import React from 'react';

// This component has been deprecated in the new design.
// Keeping an empty component to avoid breaking imports if lazy-loaded,
// though App.tsx no longer renders it.
const FounderSection: React.FC = () => {
  return null;
};

export default FounderSection;